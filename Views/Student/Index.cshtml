@model IEnumerable<ENSC.Models.Student>

@{
    ViewData["Title"] = "Students";
}


<div class="row">
    <div class="col-9">
        <h2>Les étudiants</h2>
        <table class="table">
            <thead>
                <tr>
                    <th>
                        @Html.DisplayNameFor(model => model.Name)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.EmailAdress)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.Promo)
                    </th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                    <tr>
                        <td>
                            @Html.DisplayFor(modelItem => item.Name)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.EmailAdress)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.Promo)
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
    <div class="col">
        @Html.Partial("~/Views/Student/Create.cshtml")
    </div>
</div>